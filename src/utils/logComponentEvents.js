// Logs information sent from the component to the console
// Called in the component initialization part

export function logComponentInitialization(prefix, componentName) {
  console.info(`${prefix}${componentName}`);
}
